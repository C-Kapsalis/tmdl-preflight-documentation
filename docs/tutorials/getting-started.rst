Getting started
===============

.. Diátaxis tutorial: one linear path, no branching, no "or you could
   instead". Port from tmdl-preflight-docs/tutorials/getting-started.md.
   Golden path already proven in that draft: install, copy the clean
   bike-shop fixture, run ``check`` clean, break three things by hand, run
   ``check`` again to see the three named violations, run ``fix``, confirm
   ``check`` is clean again. Show each command and its exact output as two
   separate code blocks, per the CLI archetype notes.

Install
-------

.. Command block, then its output as a separate block.

Run the bundled example
------------------------

.. The clean fixture, then the pre-broken one. End on ``fix`` clearing all
   three auto-fixable violations, leaving only the S001 advisory.
