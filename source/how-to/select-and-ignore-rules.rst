Select and ignore specific rules
================================

.. NEW PAGE, no existing draft. Real capability (--select/--ignore on the CLI, select=/ignore= on the pytest fixture and the plain Python API) with no task-oriented page anywhere. Cover all three surfaces in one page since the semantics are identical across them.
